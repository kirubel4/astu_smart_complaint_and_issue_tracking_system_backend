import type { User } from '../modules/user/user.types'; // adjust path if needed

declare global {
  namespace Express {
    interface Request {
      user?: {
        id: string;
        email: string;
        role: string;
      };
    }
  }
}

export {};